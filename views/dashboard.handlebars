{{#each blogs}}
<div class="card my-2">
  <div class="card-header navbar-dark bg-secondary d-flex justify-content-between align-items-center">
    <p>{{title}}</p>
    <p class="mt-3 ms-3">{{date_created}}</p>
  </div>
</div>
{{/each}}